# 小米前端一面

> 面试时间：2026 年 9 月 15 日  
> 岗位：前端开发实习生  
> 团队：小米基础技术平台部前端团队  
> 技术栈：React 及周边生态  
> 面试时长：约 55 分钟  
> 说明：本文根据面试录音的飞书妙记整理，部分问题由纪要中的对话语义还原，表述不一定与面试官原话完全一致。

---

## 一、自我介绍与个人背景

1. 请先做一下自我介绍。
2. 平时主要使用哪些前端技术栈？
3. 平时会通过哪些渠道学习和关注新的技术？

---

## 二、CSS — Flex 布局

4. CSS 平时使用得多吗？
5. 使用 Flex 布局怎么实现水平居中和垂直居中？
6. `flex` 属性是哪几个属性的简写？
7. `flex-grow`、`flex-shrink`、`flex-basis` 分别表示什么？

---

## 三、JavaScript — Set、Map 与 Object

8. JavaScript 中使用过 `Set` 和 `Map` 吗？
9. `Set` 和 `Map` 分别是什么数据结构？实际项目中有没有使用过？
10. `Map` 和 `Object` 有什么区别？
11. `Map` 和 `Object` 在 key 的类型上有什么区别？

---

## 四、JavaScript — Event Loop

12. 看下面这段事件循环相关代码，最终的输出顺序是什么？
13. 为什么会得到这个输出顺序？
14. 同步任务、宏任务和微任务的执行顺序是什么？
15. `Promise` 与 `setTimeout` 在事件循环中的执行优先级有什么区别？

> 面试过程中最终讨论得到的输出顺序为：`3 5 2 1 4`。

---

## 五、React — useState 与状态更新

16. 点击加一按钮之后，这段 React 代码会打印什么？
17. `useState` 更新状态后，为什么当前函数中的 `count` 不会立即变化？
18. 组件重新渲染时打印的 `count` 和 `setTimeout` 回调中打印的 `count` 有什么区别？
19. React 中状态更新的机制是怎样的？

---

## 六、React — 组件卸载与异步请求

20. 如果组件发起一个异步请求，但是请求还没有返回时组件已经卸载，会出现什么问题？
21. 这种情况下应该怎么处理？
22. 如何取消一个已经发出的网络请求？

> 这一题面试时能想到“组件卸载时取消请求”的思路，但没有及时说出 `AbortController`。

---

## 七、算法题 — 找重复数字与缺失数字

23. 给定一个由 `1 ~ 10` 组成的乱序数组，其中一个数字重复、一个数字缺失，请找出这两个数字。
24. 先说一下你的解题思路，再写代码实现。
25. 使用 `Set` 应该怎么解决这个问题？
26. 你的解法时间复杂度是多少？
27. 你的解法空间复杂度是多少？

### 当时的思路

- 第一次遍历数组，使用 `Set` 判断并找出重复数字。
- 再遍历 `1 ~ 10`，判断哪个数字不在 `Set` 中，从而找出缺失数字。
- 时间复杂度：`O(N)`。
- 空间复杂度：`O(N)`。

---

## 八、实习项目 — 自动化页面性能评测

28. 介绍一下你在实习期间做的自动化页面性能评测工具。
29. 为什么需要自己开发这样一个工具？
30. 公网页面和内网页面的性能评测分别是怎么实现的？
31. 公网页面为什么使用 PageSpeed Insights API？
32. 内网页面无法直接通过 PSI 测试时，你是怎么解决的？
33. Playwright / 无头浏览器在这个方案中承担什么作用？
34. 如何让多次 Lighthouse 测试得到的结果更稳定？
35. 为什么需要进行多轮测试并取平均值？

### 项目背景

- 公网页面：调用 PageSpeed Insights API。
- 内网页面：通过 Playwright 调用 Chrome 无头浏览器进行自动化测试。
- 多轮运行后获取性能指标，并取平均值作为最终结果。

---

## 九、前端性能优化 — 首屏 5s → 3s

36. 你提到把页面首屏加载时间从 5 秒优化到 3 秒，具体做了哪些优化？
37. 页面性能优化过程中主要关注哪些 Lighthouse 指标？
38. 如果 LCP 指标比较差，你会从哪些方向进行排查和优化？
39. `preload` 在首屏性能优化中的作用是什么？
40. 如何判断是否存在 JS / CSS 阻塞首屏渲染的问题？
41. FCP 指标比较差时，你会怎么优化？
42. 非关键资源应该如何处理？
43. `prefetch` 可以应用在什么场景？

---

## 十、前端性能优化 — JS 资源体积

44. 如果页面的 JavaScript 文件体积很大，可以从哪些方向优化？
45. 什么是 Code Splitting？
46. 什么是 Tree Shaking？
47. Gzip 为什么能够减少资源加载时间？

> 面试中回答的主要方案：
>
> - Code Splitting
> - Tree Shaking
> - Gzip 压缩

---

## 十一、前端性能优化 — 渲染性能

48. 除了网络请求和资源体积，你有没有做过渲染层面的性能优化？
49. 如果页面存在大量列表数据导致渲染卡顿，你会怎么处理？
50. 虚拟列表主要解决什么问题？

---

## 十二、项目 — Multi-Agent 智能学习系统

51. 介绍一下你的 Multi-Agent 智能学习系统。
52. 这个项目主要解决什么问题？
53. 系统整体的使用流程是什么？
54. 为什么选择 Multi-Agent 架构？
55. 系统中一共有几个 Agent？分别负责什么？

### Agent 划分

- Router Agent：负责语义识别和问题路由。
- Planner Agent：负责复杂问题规划。
- Retriever Agent：负责知识检索。
- Generator Agent：负责内容生成。
- Evaluator Agent：负责结果验收。

---

## 十三、Multi-Agent — 路由与调度

56. 所有用户问题都会经过完整的 Agent 链路吗？
57. 简单问题和复杂问题是怎么区分的？
58. 为什么简单问题不进入完整的 Multi-Agent 流程？
59. Query 的复杂度是通过什么方式判断的？
60. 为什么选择通过 LLM + Prompt 判断，而不是单独写关键词匹配规则？
61. 这样设计对 Token 消耗有什么帮助？

### 当时的调度思路

```text
简单问题
    ↓
直接调用通用 LLM

复杂问题
    ↓
Router
    ↓
Planner
    ↓
Retriever
    ↓
Generator
    ↓
Evaluator
```

---

## 十四、Multi-Agent — 模型选型

62. 不同 Agent 是否都使用同一个模型？
63. 为什么不同类型的 Agent 要选择不同规模的模型？
64. 哪些场景可以使用小参数模型？
65. 哪些任务更适合使用能力更强的大模型？

> 当时的设计思路：
>
> - 意图识别等轻量任务使用小模型。
> - Planner、Generator、Evaluator 等复杂任务使用更强的模型。
> - 在效果和调用成本之间做平衡。

---

## 十五、RAG / LightRAG

66. 为什么这个项目需要使用 RAG？
67. 为什么选择 LightRAG？
68. LightRAG 相比普通的语义检索 / 关键词检索有什么优势？
69. 为什么不直接把完整课程文档全部放进大模型上下文？
70. 你提到检索效果提升了约 15%～20%，这个结果是怎么得到的？

---

## 十六、多模态文档处理

71. 用户上传的课程资料中如果包含图片，系统怎么处理？
72. 图片内容是自己做 OCR，还是调用第三方能力？
73. 为什么选择调用成熟平台 / API，而不是自己实现 OCR 和版面还原？

---

## 十七、反问环节

我向面试官主要询问了以下问题：

1. 这次面试结果大概什么时候可以出来？
2. AI 能力越来越强的情况下，前端开发者应该怎样保持自己的核心竞争力？
3. 当前实习岗位后续是否有转正 HC？
4. 实习和秋招岗位是否可以同步推进？
5. 对于前端 / AI 应用开发方向的实习生，达到什么能力水平算比较合格？什么样的表现会比较出彩？

面试官反馈：

- 面试结果预计一周以内给出。
- 当前岗位为实习岗位，暂时无法确定后续是否存在转正 HC。
- 秋招岗位需要通过对应招聘渠道单独投递。
- 实习生首先需要具备独立完成正常业务需求的能力。
- 如果能够主动发现基建问题、提出优化方案，或者利用 AI 提升前端开发和产品迭代效率，会更加出彩。

---

## 十八、个人复盘

### 1. Flex 基础还需要再熟悉

需要重新梳理：

- `justify-content`
- `align-items`
- `flex-grow`
- `flex-shrink`
- `flex-basis`
- `flex: 1`

### 2. React 状态更新机制需要继续加强

需要重点理解：

- `useState`
- render 与状态快照
- 闭包
- `setTimeout` 中状态值的变化

### 3. 异步请求取消 API 没有及时想起来

对于 `fetch` 请求可以使用：

```js
const controller = new AbortController();

fetch(url, {
  signal: controller.signal
});

return () => {
  controller.abort();
};
```

核心 API：`AbortController`。

### 4. 性能优化是本次面试的重点

后续需要继续准备：

- LCP / FCP / Speed Index / CLS
- preload / prefetch
- JS / CSS 阻塞
- Code Splitting
- Tree Shaking
- Gzip
- Lighthouse
- PageSpeed Insights
- Playwright 自动化性能评测
- 虚拟列表

### 5. AI 项目不能只讲架构，还要准备数据依据

类似下面的问题需要准备得更扎实：

> “你说效果提升了 15%～20%，这个提升是怎么评测出来的？”

后续回答时需要明确：

- 测试集规模
- Baseline
- 评测指标
- 测试方法
- 实验前后结果

---

## 十九、本场面试重点

```text
自我介绍
   ↓
CSS / Flex
   ↓
JavaScript 基础
   ↓
Event Loop
   ↓
React
   ↓
算法题
   ↓
实习性能优化项目
   ↓
Multi-Agent + RAG 项目
   ↓
反问
```

优先复习：

1. React `useState`、闭包、状态快照。
2. React 组件卸载与异步请求取消。
3. Event Loop、宏任务、微任务。
4. Lighthouse 指标及首屏性能优化。
5. preload / prefetch / 懒加载。
6. Code Splitting、Tree Shaking、Gzip。
7. 自动化性能评测方案。
8. Multi-Agent Router / Planner / Retriever / Generator / Evaluator。
9. Agent 的简单问题 / 复杂问题路由。
10. LightRAG 的选型原因及实际评测方式。
