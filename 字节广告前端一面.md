# 字节广告前端一面

> 面试时间：2026 年 9 月 22 日  
> 岗位：前端开发（面试官确认系统中为实习岗位）  
> 团队：广告中台部门  
> 面试时长：约 57 分钟  
> 面试形式：项目经历 + 前端基础 + AI / RAG + Coding + 反问  
> 说明：本文根据完整面试录音逐字稿整理，对部分语音识别错误和口语化表达进行了修正，问题顺序基本按照实际面试流程保留。

---

## 一、自我介绍与个人背景

1. 请先做一下自我介绍。
2. 目前是大四刚开学吗？
3. 这次投递的是实习岗位吗？

---

## 二、实习经历 — CMS Skill 开发

4. 介绍一下实习期间 CMS 项目的背景，以及你主要负责了哪些工作？
5. CMS Skill 主要解决什么问题？
6. 为什么需要把 CMS 的接口、鉴权、Token 等能力封装成 Skill？
7. 你在 CMS Skill 建设中具体做了哪些工作？
8. 原来的 Skill 存在哪些问题？为什么需要补充执行脚本和执行流程？
9. 你一共搭建或者完善了哪些类型的 Skill？
10. 页面修改类 Skill 的完整执行流程是什么？
11. 页面查询类 Skill 可以通过哪些信息定位页面？
12. 页面查询完成后，会返回哪些页面 / 模板信息？

### 当时主要介绍的 Skill

- **页面查询 Skill**
  - 根据页面 ID、站点、页面名称关键词等定位页面。
  - 查询页面对应的模板、头部公共块等信息。

- **页面修改 Skill**
  - 根据用户 Query 定位目标页面。
  - 读取页面 HTML / CSS。
  - 根据需求进行针对性修改。

---

## 三、实习经历 — 自动化页面性能评测

13. 自动化性能评测也是通过 Skill 实现的吗？
14. 为什么当时需要自己搭建一套自动化性能评测工具？
15. 为什么不能直接人工使用 Lighthouse 一次完成性能测试？
16. Lighthouse 多次测试的结果为什么会存在波动？
17. 公网页面和内网 / 本地页面分别是怎么进行性能测试的？
18. 公网页面为什么可以直接通过 API 进行测试？
19. 内网页面无法直接通过公网 API 测试时，你是怎么解决的？
20. CMS 登录态在自动化测试过程中是如何复用的？

---

## 四、Web Performance — 性能指标采集

21. `FCP`、`LCP`、`CLS` 分别代表什么？
22. 公网页面的 FCP / LCP / CLS 等指标是怎么采集出来的？
23. 内网或者本地页面的性能指标是怎么采集的？
24. 如果不使用 Lighthouse，只在普通前端项目中采集这些性能指标，可以通过浏览器什么 API 实现？
25. `PerformanceObserver` 的作用是什么？

---

## 五、React — 组件频繁重渲染

26. 你自己的项目主要使用什么版本的 React？
27. 如果一个 React 页面中某个组件在不断重渲染，你会怎么定位这个问题？
28. React DevTools 可以如何帮助定位组件的重渲染问题？
29. 找到频繁重渲染的组件之后，下一步应该检查什么？
30. `React.memo`、`useMemo`、`useCallback` 分别可以从什么角度减少不必要的渲染？
31. 如果组件已经使用了 `React.memo`，函数也使用了 `useCallback`，但组件还是不断重渲染，可能是什么原因？
32. 组件自身的 `state` 为什么可能造成循环更新甚至无限重渲染？
33. `React.memo` 对 `props` 的比较方式是什么？
34. 为什么父组件每次创建新的对象、数组等引用类型，也可能导致使用了 `React.memo` 的子组件重新渲染？
35. 你在自己的实际项目中有没有真正遇到过组件无限重渲染的问题？

### 这一部分面试官重点关注

```text
组件频繁 Render
      ↓
React DevTools / Profiler 定位
      ↓
检查 State 更新
      ↓
检查 Props
      ↓
检查引用类型是否变化
      ↓
检查 useEffect / 状态更新是否形成循环
```

---

## 六、项目 — Multi-Agent + RAG 智能学习系统

36. 介绍一下 Multi-Agent + RAG 智能学习系统的项目背景。
37. 为什么会想到做这样一个期末复习系统？
38. 为什么这个系统需要使用 Multi-Agent？
39. 为什么生成思维导图、题目以及复杂问答这类任务不直接交给单个 LLM 完成？
40. Planner Agent 在复杂任务中主要解决什么问题？

---

## 七、Multi-Agent — 为什么使用工作流

41. 为什么选择 Router → Planner → Retriever → Generator → Evaluator 这样的工作流？
42. 为什么不直接使用一个 Agent，通过一个 Prompt 告诉它第一步、第二步、第三步分别做什么？
43. 单 Agent + 长 Prompt 的方式可能存在哪些问题？
44. LLM 本身具有概率性，这一点会对长链路任务产生什么影响？
45. 将复杂任务拆成多个节点有什么好处？

### 面试官真正想问的核心

不是：

> “五个 Agent 分别负责什么？”

而是：

> **“为什么要拆成工作流？为什么不让一个 Agent 一次完成？”**

---

## 八、Multi-Agent — 工作流具体如何实现

46. 这套 Multi-Agent 工作流具体是怎么搭建起来的？
47. 最开始的 MVP 版本和后面的 Agent 版本有什么区别？
48. 一个用户 Query 进入系统之后，完整的处理流程是什么？
49. Router Agent 如何判断一个问题是简单问题还是复杂问题？
50. 简单问题为什么可以跳过 Planner？
51. 复杂问题进入 Planner 后，后续如何连接 Retriever 和 Generator？
52. Evaluator Agent 的作用是什么？
53. Evaluator 判断结果不合格之后，是否会重新进入前面的流程？

### 当时介绍的整体链路

```text
                  ┌── 简单问题 ──→ Retriever / Generator
                  │
Query → Router ───┤
                  │
                  └── 复杂问题 ──→ Planner
                                      ↓
                                  Retriever
                                      ↓
                                  Generator
                                      ↓
                                  Evaluator
                                      ↓
                                不通过则重新处理
```

---

## 九、Multi-Agent — Agent 之间如何通信

54. Router Agent 的输出具体是什么？
55. LLM 的输出如何转换成下一个 Agent 可以使用的数据？
56. 为什么 Agent 之间需要统一结构化输入和输出？
57. 是否使用 JSON 作为 Agent 之间的数据格式？
58. 一个 Agent 的输出进入下一个 Agent 前，需要做哪些前处理 / 后处理？
59. 如果以后新增 Agent 或者新增旁路分支，这套工作流是否能够快速扩展？
60. 为什么统一的数据结构可以降低新增节点的成本？
61. 如何实现某些 Query 跳过部分 Agent，只执行部分工作流？

---

## 十、LLM — JSON 结构化输出与异常处理

62. 大模型输出 JSON 时，如果出现字段缺失怎么办？
63. 如果模型输出的 JSON 本身不合法怎么办？
64. 如何通过 Prompt 对模型的输出结构进行约束？
65. 为什么还需要在代码层使用 `JSON Schema` 进行二次校验？
66. 如果出现字段缺失或者字段类型错误，系统应该怎么处理？
67. Schema 校验失败之后，是否会让模型重新生成？
68. 重试次数应该如何限制？
69. 如果连续多次重试仍然失败，系统如何进行降级处理？
70. 为什么需要把具体失败任务或者 Task ID 暴露给用户？
71. 如果处理的是 Excel / 表格任务，怎么告诉用户具体是哪几行数据出现了问题？

### 当时的兜底链路

```text
LLM 输出
   ↓
JSON Schema 校验
   ↓
通过 ─────────→ 下一个节点
   ↓
失败
   ↓
重新交给 LLM 修复
   ↓
限制最大重试次数
   ↓
仍失败
   ↓
降级 + 返回错误信息 / Task ID
```

---

## 十一、Dify — Workflow 与 Agent 如何选择

72. Dify 项目使用的是 Workflow 模式吗？
73. Dify 同时也提供 Agent 模式，为什么这个项目选择 Workflow？
74. Workflow 和 Agent 分别适合什么类型的任务？
75. 如果任务流程非常明确，例如表格分类 → 字段提取 → 回填，为什么更适合 Workflow？
76. 什么类型的任务更适合 Agent？
77. 当任务需要自主规划、调用多个 API、循环重试时，Agent 相比 Workflow 有什么优势？

---

## 十二、RAG — LightRAG 技术选型

78. Multi-Agent 项目的 RAG 部分是怎么做的？
79. 为什么选择 LightRAG？
80. LightRAG 相比普通 RAG 有什么特点？
81. LightRAG 中高层关键词和低层关键词分别解决什么问题？
82. 高层关键词通常对应什么类型的信息？
83. 低层关键词通常对应什么类型的信息？
84. 两种检索结果最后是怎么组合的？

---

## 十三、RAG — 多路检索与 RRF

85. 除了 LightRAG，本项目还使用了哪些检索方式？
86. `pgvector` 在检索过程中承担什么作用？
87. 关键词检索和向量检索分别有什么优势？
88. 为什么需要将 LightRAG、向量检索、关键词检索组合起来？
89. 多路检索结果是怎么进行融合的？
90. `RRF` 是什么？在这个项目中解决什么问题？
91. 多路检索之后最终返回给模型的内容是什么？
92. 最终召回的是整篇文档还是 Chunk？

### 当时介绍的检索结构

```text
                  ┌─ LightRAG
                  │
Query ────────────┼─ pgvector / Embedding
                  │
                  └─ Keyword Search
                         ↓
                       RRF
                         ↓
                      重排序
                         ↓
                       Chunk
                         ↓
                        LLM
```

---

## 十四、RAG — Chunk 上下文丢失问题

93. 假设一个 PDF 有 600 多页，被切成大量 Chunk，Query 最终只召回其中几个 Chunk，会出现什么问题？
94. 如果召回的 Chunk 与它前后内容存在强上下文关系，只把当前 Chunk 给模型是否足够？
95. 如何解决 Chunk 被截断导致上下文不完整的问题？
96. 是否可以根据 `page_id` 或 Chunk ID 向前、向后扩展上下文？
97. Chunk 切分时为什么通常需要设置 Overlap？
98. 如何判断是否需要补充相邻 Chunk？

---

## 十五、RAG — 父子索引

99. 了解 Dify 中的父子索引 / Parent-Child Chunk 吗？
100. 什么是父 Chunk？
101. 什么是子 Chunk？
102. 为什么检索时可以使用较小的子 Chunk，而返回上下文时使用较大的父 Chunk？
103. 父子索引能够解决普通 Chunk 检索中的什么问题？
104. 你在自己的项目中实际使用过父子索引吗？

> 这一题当时回答：看过 Dify 相关文档，但项目中没有实际使用。

---

## 十六、RAG — 知识图谱

105. 对于课程文档、图片等知识类资料，有没有考虑构建知识图谱？
106. 知识图谱是否能够提高这类知识型数据的召回效果？
107. LightRAG 本身和知识图谱之间有什么关系？
108. LightRAG 是如何抽取实体及实体之间关系的？
109. 图结构对于跨段落、跨 Chunk 的知识关系有什么帮助？
110. 为什么 LightRAG 在一定程度上能够补充普通向量检索缺少的上下文关系？

---

## 十七、算法题 — 二叉树路径数字求和

111. 给定一个二叉树，从根节点到每个叶子节点都会组成一个数字，求所有路径对应数字的总和。

例如：

```text
        1
       / \
      2   3
     / \
    4   5
```

可以得到：

```text
124
125
13
```

最终需要返回：

```text
124 + 125 + 13
```

112. 这道题可以使用什么遍历方式实现？
113. 在 DFS 过程中如何记录当前路径形成的数字？
114. 如何判断当前节点是不是叶子节点？
115. 时间复杂度和空间复杂度分别是多少？

> 面试现场使用 Python 完成，代码运行结果正确。

---

## 十八、JavaScript 手写题 — EventBus

116. 实现一个简单的 `EventBus` 类，需要包含以下三个方法：

```js
on(eventName, callback)

off(eventName, callback)

emit(eventName, ...args)
```

要求：

- 同一个 `eventName` 可以绑定多个 Callback。
- 支持删除指定 Callback。
- `emit` 时按照订阅顺序执行。
- `emit` 的参数需要继续传递给 Callback。
- 不需要考虑异步 Callback。
- 不需要考虑 Callback 内部抛出异常的情况。

---

## 十九、EventBus — 连续追问

117. 如果同一个 `eventName` 多次注册同一个 `callback`，内部会保存几个 Callback？
118. 按照你当前 `off` 的实现，删除这个 Callback 时，是删除第一个还是全部删除？
119. 为什么当前使用 `filter` 的实现会把所有相同 Callback 都删除？
120. `filter` 在执行过程中是否会遍历整个 Callback 数组？
121. 如果需求变成“只删除第一个满足条件的 Callback”，应该怎么实现？
122. 是否可以先通过循环或者 `findIndex` 找到第一个 Callback 的下标？
123. 找到下标后如何只删除该元素？
124. 使用 `splice` 与 `filter` 在这个场景下有什么区别？

### 面试现场原实现对应的行为

```js
callbacks = callbacks.filter(cb => cb !== callback);
```

如果同一个函数被注册多次：

```js
on('event1', callback1);
on('event1', callback1);
on('event1', callback1);
```

执行：

```js
off('event1', callback1);
```

会将三个 `callback1` **全部过滤掉**。

如果只删除第一个，可以考虑：

```text
findIndex / 循环
      ↓
找到第一个满足条件的下标
      ↓
splice 删除
      ↓
结束
```

---

## 二十、反问环节 — 团队业务

我向面试官询问：

### 1. 广告中台团队目前主要负责什么业务？

面试官介绍：

- 团队属于 TikTok 广告平台下面的中台团队。
- 广告平台面向企业、公司以及个人广告主。
- 整个平台下面包含很多子应用和子模块。
- 团队主要负责广告平台的中台能力建设。
- 前端相关工作包括：
  - SDK
  - 组件库
  - 前端工程化
  - 一些底层工具链
  - AI 与业务结合的相关能力

---

## 二十一、反问环节 — AI 对前端的影响

### 2. AI 能力快速发展之后，对前端以及中台团队会产生什么影响？

面试官主要分享了几个观点：

```text
传统前端
↓
业务页面 / 表单 / 页面功能开发
↓
AI 对纯页面开发效率产生明显影响
↓
前端逐渐向更完整的全栈角色发展
```

前端的一个重要优势是：

> **相比单纯关注某一端，更接近用户，也更了解完整的用户旅程。**

未来可以将这些业务知识和用户操作流程进一步沉淀给 Agent，让 Agent：

- 理解用户如何使用平台。
- 理解页面操作之后后端发生了什么。
- 优化用户操作路径。
- 参与实际业务开发。
- 协助完成端到端需求。

---

## 二十二、后端能力追问

125. 你对 Go / Golang 有了解吗？
126. 后端开发平时主要使用什么技术栈？
127. 是否主要使用 Python + FastAPI？

> 当时回答：Go 接触较少，目前后端主要使用 Python / FastAPI 技术栈。
