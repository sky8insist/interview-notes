# 海康前端一面

> 面试时间：2026 年 9 月 23 日  
> 岗位：前端开发  
> 面试时长：约 33 分钟  
> 面试形式：自我介绍 + 实习 / 项目深挖  
> 说明：本文根据完整面试录音逐字稿整理，对部分语音识别错误和口语化表达进行了修正，问题顺序基本按照实际面试流程保留。

---

## 一、自我介绍

### 1. 请先做一下自我介绍。

主要介绍：

- 前端开发和 AI 应用开发方向。
- 万兴科技 Web 前端开发实习经历。
- CMS Skill 建设和页面性能优化。
- Multi-Agent + RAG 智能学习系统。
- 基于 Dify 的政数表格智能体。

---

## 二、Web Performance — FCP / LCP 性能瓶颈

### 2. 在做首屏性能优化时，你是怎么识别 FCP 和 LCP 的性能瓶颈的？

### 3. 判断 LCP 性能问题时，除了 Lighthouse 的最终评分，还应该关注哪些具体阶段？

### 4. 如果发现 LCP 资源下载时间比较长，你会从哪些方向进行优化？

### 5. 如果发现渲染时间比较长，你会怎么判断是否存在 JavaScript / CSS 阻塞？

### 6. `preload` 在 LCP 优化过程中有什么作用？

这一部分面试官重点关注的不是“背 LCP 定义”，而是：

```text
发现 LCP 较差
    ↓
判断瓶颈到底在哪
    ↓
请求 / 下载 / 渲染
    ↓
针对不同阶段采用不同方案
```

---

## 三、前端性能优化 — 长列表 / 虚拟列表

### 7. 能不能举一个你实际遇到的性能问题，以及最后是怎么解决的？

当时举的例子：

> Dify 表格智能体需要在前端展示大规模表格数据，一次性渲染大量 DOM 导致白屏和响应时间较长，最后通过虚拟列表减少实际渲染节点数量。

### 8. 为什么一次性渲染一万行表格会产生明显的性能问题？

### 9. 虚拟列表是怎么减少 DOM 渲染数量的？

### 10. 虚拟列表中为什么需要设置固定行高或者计算每一行的位置？

### 11. 如果表格不只是展示，还需要支持行高亮、动态编辑等操作，除了虚拟列表之外还需要考虑哪些问题？

### 12. 如何保证表格数据动态修改之后，不会出现数据错行或者状态错乱？

### 13. 对于需要编辑 / 高亮的行，如何维护其状态？

### 14. `React.memo`、`useMemo`、`useCallback` 在这种场景下分别可以起到什么作用？

这部分是从“**虚拟列表能不能渲染得快**”继续追问到了：

> **虚拟列表中的数据如果还需要动态编辑，你怎么保证状态正确？**

---

## 四、前端性能优化 — Web Worker

### 15. 如果长列表中还涉及大量 JavaScript 计算，例如重新排序、数据计算等，你会把这些计算放在哪里？

### 16. 为什么不应该把大量计算全部放在主线程？

### 17. `Web Worker` 可以解决什么问题？

当时回答的核心思路：

```text
大量计算
   ↓
如果全部放在主线程
   ↓
阻塞渲染和用户交互
   ↓
Web Worker
   ↓
后台线程完成计算
```

---

## 五、RAG / LightRAG

### 18. 你的智能学习系统使用了 LightRAG，RAG / 图结构检索和传统向量检索相比，各自有什么优缺点？

### 19. 普通关键词检索存在哪些问题？

### 20. 单纯的向量语义检索又存在哪些局限？

### 21. LightRAG 相比普通检索方式最大的优势是什么？

### 22. LightRAG 为什么能够表示实体之间的关系？

### 23. LightRAG 的高层关键词和低层关键词分别解决什么问题？

### 24. 如果 LightRAG / RAG 有这些优势，为什么不是所有场景都直接使用 RAG？

### 25. RAG 在简单文档或者简单问答场景下有什么额外成本？

### 26. 随着知识库中的文档越来越多，RAG 会带来哪些维护成本？

这部分面试官明显不是只想听：

> “LightRAG 比向量检索好。”

而是在继续追问：

> **既然好，为什么不是所有场景都用？它的代价是什么？**

---

## 六、文档解析 — MinerU

### 27. 在 PDF / Office 等复杂文档解析中，为什么选择 MinerU？

### 28. MinerU 主要帮你解决了什么问题？

### 29. MinerU 相比传统 OCR / 文档解析器有什么优势？

### 30. 为什么直接使用 OCR 处理复杂课程资料效果可能不好？

### 31. MinerU 为什么能够更好地保留：

- 文档结构
- 段落
- 表格
- 公式
- 图片

### 32. 为什么最终选择将文档解析为 Markdown？

核心需求是：

```text
原始 PDF / Office / 图片
        ↓
结构化解析
        ↓
标题 / 正文 / 表格 / 公式 / 图片
        ↓
Markdown
        ↓
RAG / Agent
```

---

## 七、MinerU — 底层原理追问

### 33. 你有了解过 MinerU 是怎么做到复杂版面解析的吗？

### 34. MinerU 如何识别文档中的不同区域？

### 35. 对正文、图片、公式、表格等不同类型内容，为什么需要采用不同的解析方式？

### 36. 完成不同区域的识别之后，最终是如何重新组合成结构化 Markdown 的？

> 这一题当时明确说明：看过官方文档，但对 MinerU 的底层完整实现没有深入研究，随后按照自己的理解说明了“版面识别 → 内容分类 → 分别解析 → 重新组合”的思路。

---

## 八、异常处理 — 重试与降级

### 37. 文档解析过程中如果发生异常，你的回退策略是什么？

### 38. 哪些情况会触发重试？

例如：

- 输出为空。
- 输出异常。
- 置信度过低。
- 解析结果不符合预期。

### 39. 为什么需要限制最大重试次数？

### 40. 如果连续重试仍然失败，系统怎么做降级处理？

### 41. 降级时是返回缓存数据，还是直接向用户反馈错误？

当时整体思路为：

```text
解析
 ↓
结果检查
 ↓
正常 ───────→ 后续流程
 ↓
异常
 ↓
重试
 ↓
超过最大次数
 ↓
降级 / 缓存 / 返回错误信息
```

---

## 九、文件解析 — 实时任务与用户等待

### 42. MinerU 本身推理速度较慢，而且会消耗 GPU，那么你的 PDF / Office 文件是离线批量解析，还是用户实时上传后解析？

### 43. 如果支持用户实时上传和解析，怎么减少用户的等待感？

### 44. 为什么将文件解析放到异步任务中，而不是同步阻塞请求？

### 45. 前端如何感知文件当前处理到什么阶段？

### 46. 后端如何把任务状态同步给前端？

### 47. 是否可以通过轮询的方式获取当前任务状态？

这一段实际已经从 AI 项目追问到了典型的**异步任务系统设计**：

```text
上传文件
   ↓
创建异步任务
   ↓
立即返回 Task ID
   ↓
后台解析
   ↓
前端轮询任务状态
   ↓
Loading / Progress
   ↓
解析完成
```

---

## 十、Multi-Agent — Evaluator 与回溯机制

### 48. 你的系统采用了五阶段工作流，如果最终 Evaluator 判断结果质量不达标，应该回到哪个阶段重新执行？

### 49. 是每次都回到 Planner，还是根据具体错误类型决定？

### 50. 如果是检索结果有问题，应该回到哪个阶段？

### 51. 如果检索证据没有问题，但最终生成的回答质量不好，应该回到哪个阶段？

### 52. 如何判断究竟是 Retriever 的问题还是 Generator 的问题？

### 53. 工作流中的循环最大会执行多少次？

### 54. 为什么需要给 Agent 循环设置最大次数？

这一部分是非常典型的 Agent 工程问题：

```text
Evaluator
   ↓
判断失败原因
   ↓
┌───────────────┬───────────────┐
检索结果有问题       生成结果有问题
   ↓                   ↓
Retriever           Generator
   ↓                   ↓
重新检索             重新生成
   └─────────→ Evaluator
```

而不是简单地：

```text
失败 → 整条工作流重新跑一遍
```

---

## 十一、Dify 表格智能体 — Chunk 与上下文

### 55. 大表格为了避免 Token 超限需要进行切片，如果行与行之间存在语义关系，怎么保证切片之后上下文不会丢失？

### 56. 你的实际比赛数据中，表格行与行之间是否存在依赖关系？

### 57. 如果现实业务中的表格存在跨行上下文关系，你会怎么处理？

### 58. 是否可以通过 Overlap 的方式，让前一个切片和后一个切片保留部分重复内容？

### 59. 在检索或者处理过程中，是否需要补充前后相关数据？

> 当时先明确说明：项目实际数据中每一行基本独立，因此比赛项目本身不存在明显的跨行语义依赖；随后再回答如果存在这种关系，可以通过上下文重叠和相邻内容补充处理。

---

## 十二、LLM — JSON Schema 与结构化输出

### 60. 你使用 JSON Schema 做模型结构化输出，但模型仍可能出现字段缺失或者 JSON 不合法，怎么处理？

### 61. 第一次 Schema 校验失败之后，系统应该怎么处理？

### 62. 是否会将错误结果重新交给模型进行修复？

### 63. 第二次仍然失败时，是继续重试还是直接降级？

### 64. 如何向用户说明本次任务为什么失败？

### 65. 为什么需要给异常任务分配 Task ID？

### 66. 如何判断异常来自：

- 用户输入
- Prompt
- 模型输出
- JSON Schema
- 程序解析

当时给出的处理思路是：

```text
模型输出
   ↓
JSON Schema
   ↓
失败
   ↓
重新生成 / 修复
   ↓
再次校验
   ↓
仍失败
   ↓
标记任务失败
   ↓
返回 Task ID
```

---

## 十三、线上问题 — 可观测性与异常追踪

### 67. 系统真正给用户使用之后，如果出现本地无法复现的偶发大模型异常，你怎么排查？

### 68. 在系统设计中有没有考虑便于线上调试和问题追溯的机制？

### 69. 如何知道某一次异常究竟发生在哪一个任务？

### 70. 如何通过 Task ID 追踪一次完整的模型调用？

### 71. 是否需要记录每个任务的输入和输出？

### 72. 如何判断问题到底出现在：

```text
原始文件
   ↓
输入结构化
   ↓
LLM 调用
   ↓
模型输出
   ↓
结构校验
```

### 73. 为了进一步增强线上可观测性，还可以在哪些节点增加校验和日志？

这应该是本场比较值得复盘的一组问题，因为面试官已经从：

> **“失败了怎么办？”**

继续追到了：

> **“线上偶现失败，本地复现不了，你怎么找到问题？”**

---

## 十四、产品 / 需求调研

### 74. 这个智能学习系统不仅是你开发的，也是你自己做需求调研的。在采访同学的过程中，有没有发现一些最开始没有想到的需求？

### 75. MVP 版本上线给同学体验之后，他们主要反馈了什么问题？

### 76. 最开始只使用关键词检索时，为什么无法很好地覆盖多份课程文档？

### 77. 用户反馈检索效果不好之后，你是如何改进的？

### 78. 为什么后来选择：

- 关键词检索
- 向量语义检索
- LightRAG

三路检索？

### 79. 多路检索结果最后是如何融合和重排的？

当时的迭代过程比较清晰：

```text
MVP
 ↓
关键词检索
 ↓
用户测试
 ↓
发现多文档召回不足
 ↓
Keyword + Vector + LightRAG
 ↓
融合 / 重排
 ↓
再次用户测试
```

---

## 十五、用户反馈 — OCR / 手写资料问题

### 80. 用户在实际使用课程资料解析时，还提出了什么你最开始没有考虑到的问题？

### 81. 为什么普通 OCR 对手写内容、模糊字体或者复杂排版支持不好？

### 82. 针对这种问题，你后来做了哪些改进？

### 83. MinerU 解析完成之后，如果仍然存在模糊字段，可以怎么进一步处理？

### 84. 大模型仍然无法正确识别时，是否可以结合上下文语义进行兜底？

这部分同样属于：

> **用户反馈 → 发现问题 → 技术方案迭代**

而不是单纯介绍技术栈。
